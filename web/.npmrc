# next-auth@5 beta declares a peer dep of next@^14||^15, but we run next@16.
# Until next-auth marks Next 16 supported, install with legacy peer resolution
# (same as our local `npm install --legacy-peer-deps`). Required for CI/Netlify.
legacy-peer-deps=true
